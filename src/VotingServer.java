import server.BasicServer;

import java.io.IOException;

public class VotingServer extends BasicServer {

    protected VotingServer(String host, int port) throws IOException {
        super(host, port);
    }
}
